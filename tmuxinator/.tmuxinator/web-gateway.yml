name: web-gateway
root: ~/Code/web-gateway

windows:
  - editor:
      layout: main-vertical
      panes:
        - ls && NVIM_APPNAME=nvim-astro nvim 
  - console:
      panes:
        - ls && devbox shell
  - claude:
      panes:
        - ls && devbox shell
  - devbox_services:
      panes:
        - ls && [[ "$DEVBOX_SERVICES_UP" == "true" ]] && devbox services up || true
