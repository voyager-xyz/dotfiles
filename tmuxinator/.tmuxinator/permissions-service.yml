name: permissions-service
root: ~/Code/permissions-service

windows:
  - editor:
      layout: main-vertical
      panes:
        - ls && NVIM_APPNAME=nvim-astro nvim
  - console:
      panes:
        - ls 
