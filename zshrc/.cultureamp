alias dsu="devbox services up"

source "${HOME}/.config/hotel/config.zsh"

# granted 
. "$HOME/.cargo/env"
fpath=("${HOME}.granted/zsh_autocomplete/assume/" $fpath)
fpath=("${HOME}/.granted/zsh_autocomplete/granted/" $fpath)
alias assume=". assume"
export PATH="$HOME/.local/bin:$PATH"
alias g="gradle"
alias d="devbox"
